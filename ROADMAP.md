# IPTV Web Player — 迭代路线规划（v12~v15）

> 评估日期：2026-05-17
> 当前版本：v11
> 规划原则：**用户价值优先、避免过度工程化、渐进式增强**

---

## 一、WebSocket 深度评估

### 1.1 当前痛点
- 175 频道首次加载大量显示 "?"（unknown），需等 5~15s 后台检测
- 前端 60s 轮询 playlist hash，有延迟且产生持续 HTTP 开销
- 频道状态变更（如从 green 变 red）用户无感知，需手动刷新

### 1.2 WebSocket 能带来的好处
| 能力 | 价值 | 适用性 |
|------|------|--------|
| 实时推送健康检查结果 | 后台检测完立即更新卡片状态，无需刷新 | ✅ 高 |
| 即时 playlist 更新通知 | 源变化秒级触达 | ✅ 高 |
| 双向控制 | 服务端主动要求客户端刷新/跳转 | ⚠️ 本项目用不上 |
| 减少 HTTP 连接开销 | 长连接替代频繁轮询 | ✅ 中（家庭场景连接数少）|

### 1.3 SSE（Server-Sent Events）比 WebSocket 更适合本项目

| 维度 | WebSocket | SSE | 结论 |
|------|-----------|-----|------|
| 通信方向 | 双向 | 单向（server→client）| IPTV 播放器本质单向接收，SSE 够用 |
| 底层协议 | `ws://` 自定义协议 | 标准 HTTP | SSE 兼容现有 OpenResty，无需改 nginx |
| 自动重连 | 需手动实现（心跳+重连）| 浏览器原生 `EventSource` | SSE 更可靠，代码更少 |
| Flask 支持 | 需 Flask-SocketIO / gevent | 纯生成器 `yield` 即可 | SSE 零额外依赖 |
| 代理穿透 | 需处理 `Upgrade` 头 | 就是普通 HTTP | SSE 穿透防火墙/企业代理更容易 |
| 实现耗时 | 半天~1 天 | 1~2 小时 | SSE 成本低 5 倍+ |

> **核心洞察**：本项目的数据流是"服务器推送状态更新，客户端被动接收"，这恰好是 SSE 的设计场景。WebSocket 的双向通信能力对 IPTV 播放器是过度设计。

### 1.4 WebSocket / SSE 结论

**不推荐 WebSocket，如需要实时推送改用 SSE。**

当前 60 秒轮询在单机家庭场景下完全够用，SSE 可作为 v13 的优化项，非紧急。

---

## 二、FLV 支持复杂度评估

### 2.1 技术选型

| 库 | 状态 | 大小 | 支持格式 |
|----|------|------|----------|
| **flv.js** | ⚠️ 停止维护（5 年未更新）| ~180KB | FLV only |
| **mpegts.js** | ✅ 活跃维护（v1.8.0, 2024-12）| ~267KB min (~80KB gzip) | FLV + MPEG2-TS |

**选型：mpegts.js**（flv.js 的继承者，更现代，维护活跃）

### 2.2 实现改动量

**前端（主要改动）**：
```javascript
// 新增格式路由逻辑
function playChannel(url, name) {
  const format = detectFormat(url); // 后端返回或前端推断
  
  if (format === 'hls') {
    // 现有 hls.js 逻辑（不变）
  } else if (format === 'flv') {
    // 新增 mpegts.js 逻辑
    const mpegPlayer = mpegts.createPlayer({
      type: 'flv',
      isLive: true,
      url: proxyUrl(url),
      cors: true,
    });
    mpegPlayer.attachMediaElement(player);
    mpegPlayer.load();
    mpegPlayer.play();
    
    // 需要处理：错误事件、销毁、超时 fallback
  }
}
```

**后端（极小改动）**：
- 代理层已支持流式传输，无需修改
- 可选：在 `/api/health` 或 `/api/proxy` 增加 `X-Stream-Format` 响应头

### 2.3 风险与成本

| 层面 | 成本 | 风险 |
|------|------|------|
| JS 体积 | +267KB（gzip ~80KB）| 移动端加载增加 ~0.3~0.5s |
| 播放器兼容 | mpegts.js 需 MSE 支持 | iOS < 17 可能无法播放 FLV |
| 生命周期管理 | 需同时维护 hls.js + mpegts.js 两个实例 | 内存泄漏、事件冲突 |
| 调参复杂度 | FLV 直播流的延迟、缓冲、seek 行为与 HLS 不同 | 可能出现卡顿、音画不同步 |
| 测试覆盖 | 需增加 FLV 格式的端到端测试 | — |

### 2.4 投入产出比

- **受影响频道**：仅 2~3 个（TVBS精采、华艺中文等 iptv.4666888.xyz 源）
- **FLV 是逐渐淘汰的格式**，IPTV 行业正向 HLS/DASH 迁移
- **替代方案**：这些频道通常有 HLS 同源（只是当前 M3U 没收录）

### 2.5 FLV 结论

**延后实现**。当前投入产出比过低。建议：
1. 先在 M3U 源层面寻找这些频道的 HLS 替代源
2. 如确实需要，采用"按需加载"策略：仅在检测到 FLV 时才动态加载 mpegts.js（而非全局引入）
3. 评估时机：当 FLV 频道数量 > 10 或用户明确需要时

---

## 三、综合迭代计划

### 📊 功能价值/复杂度矩阵

```
高价值 ↑
        │  EPG节目单    │  搜索功能
        │  (中复杂度)   │  (低复杂度)
        │               │
        │───────────────┼───────────────
        │  收藏/历史    │  SSE推送
        │  (低复杂度)   │  (中复杂度)
        │               │
        │───────────────┼───────────────
        │  FLV支持      │  多源聚合
        │  (中复杂度)   │  (高复杂度)
        │               │
        └───────────────┴───────────────→ 高复杂度
```

### 🗓️ 迭代路线图

#### Phase 1: 体验优化 — v12（推荐立即启动，预计 1~2 天）
**目标：解决每天使用时的最大痛点（找频道、看节目）**

| # | 功能 | 用户价值 | 复杂度 | 交付标准 |
|---|------|----------|--------|----------|
| 1 | 🔍 **频道搜索** | ⭐⭐⭐⭐⭐ | 低 | 实时过滤 175 频道，支持中文/拼音/首字母 |
| 2 | 📺 **EPG 节目单** | ⭐⭐⭐⭐⭐ | 中 | 对接 epg.112114.xyz，显示"正在播放"/"即将播放" |
| 3 | ⭐ **收藏夹** | ⭐⭐⭐⭐ | 低 | localStorage 保存，顶部快捷访问栏 |
| 4 | 🕐 **播放历史** | ⭐⭐⭐ | 低 | 最近 10 个频道，一键回看 |

#### Phase 2: 实时推送 — v13（预计 2~3 天）
**目标：SSE 替代轮询，提升状态感知体验**

| # | 功能 | 说明 |
|---|------|------|
| 1 | SSE 端点 `/events` | Flask 生成器模式，发送 `text/event-stream` |
| 2 | 健康状态实时推送 | 后台检测完成后立即 push 更新到前端 |
| 3 | playlist 更新推送 | hash 变化时即时推送，替代 60s 轮询 |
| 4 | 连接状态指示 | 顶部显示 SSE 连接/断开状态 |

#### Phase 3: 多格式支持 — v14（可选，预计 1~2 天）
**目标：兼容 FLV 源**

| # | 功能 | 说明 |
|---|------|------|
| 1 | mpegts.js 按需加载 | 仅在检测到 FLV 时动态引入 |
| 2 | 播放器格式路由 | 前端根据后端返回的格式自动选择播放器 |
| 3 | FLV 错误处理 | 统一超时、重试、fallback 逻辑 |

#### Phase 4: 架构增强 — v15+（远期）
- 多 M3U 源聚合与合并
- 同一频道多 URL 自动最优选择
- 用户账户系统（多设备收藏同步）
- PWA 支持（离线缓存、添加到主屏）

---

## 四、v12 详细设计

### 4.1 频道搜索

**交互设计**：
```
┌──────────────────────────────────────────┐
│  📺 Player                               │
├──────────────────────────────────────────┤
│  [🔍 搜索频道（支持拼音）]  [⭐] [🕐]     │  ← 搜索栏+收藏按钮+历史按钮
├──────────────────────────────────────────┤
│  ⭐ CCTV-1  凤凰中文  浙江卫视  湖南卫视 │  ← 收藏+历史快捷栏（可横向滚动）
├──────────────────────────────────────────┤
│  ▼ 央视 (5)                              │
│    [CCTV-1 新闻联播 ▶] [CCTV-2 ...]      │  ← 卡片显示当前节目
│  ▼ 卫视 (8)                              │
│    [湖南卫视 你好星期六 ▶] [...]         │
└──────────────────────────────────────────┘
```

**技术方案**：
- 搜索框绑定 `input` 事件，实时过滤 `channelsData`
- 拼音匹配：引入 `pinyin` 库（轻量，~50KB）或预生成拼音映射表
- 匹配优先级：精确匹配 > 前缀匹配 > 包含匹配 > 拼音匹配
- 空结果时显示提示"未找到频道，试试拼音搜索"

### 4.2 EPG 节目单

**数据来源**：
- 主源：`https://epg.112114.xyz?ch={频道名}&date={YYYY-MM-DD}`
- 返回 JSON 格式，无需 XML 解析
- 当天数据缓存到 localStorage（TTL 6 小时）

**展示方案**：
```javascript
// 频道卡片显示当前节目
const currentProgram = getCurrentEPG(channelName); // 找当前时间段的节目
// 显示：CCTV-1
//      🎬 新闻联播

// 点击卡片弹窗显示完整节目单
```

**后端代理（解决 CORS）**：
```python
@app.route("/api/epg")
def api_epg():
    channel = request.args.get("ch")
    date = request.args.get("date")
    # 代理到 epg.112114.xyz
    # 缓存响应 6 小时
```

### 4.3 收藏 + 历史

**数据存储**：
```javascript
// localStorage 结构
{
  "iptv_favorites": ["凤凰中文", "CCTV-1", "浙江卫视"],
  "iptv_history": [
    {"name": "凤凰中文", "url": "...", "time": 1715932800},
    {"name": "CCTV-1", "url": "...", "time": 1715932700}
  ]
}
```

**UI 设计**：
- 收藏：频道卡片长按/右键 → "加入收藏"，或搜索栏旁星形按钮
- 历史：自动记录，顶部快捷栏展示最近 5 个，点击直接播放
- 快捷栏：可横向滚动，收藏频道置顶，历史频道按时间倒序

---

## 五、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| EPG API 不稳定 | 中 | EPG 功能不可用 | 增加多源 fallback（112114 / fanmingming / cntv）|
| 搜索拼音库体积大 | 低 | 前端加载慢 | 预生成频道拼音映射表（打包时生成 JSON）|
| SSE 连接数限制 | 低 | 多标签页时失败 | HTTP/2 多路复用；降级到轮询 |
| mpegts.js 兼容性问题 | 中 | FLV 频道仍无法播放 | 保留"不支持"提示，不阻塞其他功能 |

---

## 六、一句话决策

> **不做 WebSocket，如需实时推送改用 SSE；FLV 延后；v12 全力投入「搜索 + EPG + 收藏」三大功能，解决用户每天"找频道、看节目"的核心痛点。**
